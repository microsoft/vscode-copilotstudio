# LSP Journal CLI — Completion Log

Terse ledger of completed work. Append-only. Consult for context when deriving the
next action in `journal-cli-continuation.md`.

| Date | Entry | Outcome |
|------|-------|---------|
| 2026-02-04 | Core journal model: self-validating steps, expected/actual/status/diff inline | Working |
| 2026-02-04 | Unified RunCommand: record and replay are the same operation | Working |
| 2026-02-04 | CLI commands: run by name, `--all`, `--verbose`, `--force`, `repl`, `diff` | Working |
| 2026-02-04 | Server auto-discovery (`ServerLocator` finds `LanguageServerHost.exe` from build output) | Working |
| 2026-02-04 | Deterministic output normalization (sorted keys, stripped non-deterministic fields) | Working |
| 2026-02-04 | Workspace path resolution (`UriPlaceholder` — `${workspace}` tokens ↔ absolute URIs) | Working |
| 2026-02-04 | Notification capture and validation (`waitFor` + `expectedNotifications`) | Working |
| 2026-02-04 | Classification metadata (`recorded`/`normative`, `normativeReason`, `normativeReviewer`) | Working |
| 2026-02-04 | 4 workspace fixtures created: file-only, connected, all-the-things, all-the-things-clean | In place |
| 2026-02-04 | 7 initial journals passing: lifecycle, smoke-test, diagnostics, workspace-indexing, file-projection, completion-smoke, all-the-things-indexing | 7/7 pass |
| 2026-02-05 | S1 (empty completions) root cause: journal params omitted `CompletionContext`; server null-guarded and returned `[]` | Root cause found |
| 2026-02-05 | S1 fix: added `"context": { "triggerKind": 1 }` to all completion steps | Fixed; completions return items |
| 2026-02-05 | all-the-things-completions journal: 64 steps covering 20 doc types with CompletionContext | Passing |
| 2026-02-05 | all-the-things-diagnostics journal: 12 steps covering error-state files | Passing; suspects S2–S5 observed |
| 2026-02-05 | Legacy record/replay commands removed — single model, single format | Cleaned up |
| 2026-02-05 | V2 model consolidation — single `Journal.cs` with `Models` namespace | Cleaned up |
| 2026-02-05 | Pending workflow: `.pending/` directory, material change detection, accept/discard commands | Working |
| 2026-02-05 | AcceptCommand with fingerprint-based annotation merge | Working |
| 2026-02-06 | Step-level review annotations: `Review`, `ReviewNote`, `SuspectId` on `JournalStep` | Working |
| 2026-02-06 | `StepFingerprint` utility for annotation survival across rebaselines | Working |
| 2026-02-06 | `ReviewCommand`: summary, detail, `--suspects-only` filter | Working |
| 2026-02-06 | Git metadata capture: branch, commit, merge-base, branch-depth | Working |
| 2026-02-06 | REPL: 13+ step types (initialize, open, close, change, completion, diagnostics, definition, codeAction, signatureHelp, semanticTokens, etc.) | Working |
| 2026-02-06 | Priority 1 fixture expansion: RegexEntity, SharePointSearchSource, ConditionGroup, Foreach, OAuthInput, InvokeFlowTaskAction, AutonomousSettings, OnRedirect, OnError, OnActivity | Added to fixtures |
| 2026-02-06 | connectionreferences.mcs.yml added to all-the-things-workspace (matches real-world state) | Fixture updated |
| 2026-02-06 | S2 investigation: theory that `connectionreferences.mcs.yml` enables connector-ref validation was refuted; validation doesn't fire in detached mode | Investigated; remains suspect |
| 2026-02-06 | Comprehensive component type inventory: 26+ types across two all-the-things fixtures | Documented |
| 2026-02-06 | 9 journals, all passing (added all-the-things-completions and all-the-things-diagnostics) | 9/9 pass |
| 2026-02-09 | completion-depth journal: property-key in nested contexts (SetVariable, IfCondition), enum-value after change (action kinds), 4 trigger-character variants (`:` empty, ` ` enum, `.` scope prefixes, `\n` properties) | Passing; 8/8 completion steps meaningful |
| 2026-02-09 | completion-error-context journal: completions in 4 error-state files (BrokenConnector, InvalidEntity, InvalidScope, MissingFields) — server returns results despite active diagnostics | Passing; 4/4 return items |
| 2026-02-09 | 11 journals, all passing. M2 completion-depth work item complete. | 11/11 pass |
| 2026-02-09 | REPL removed: deleted `ReplCommand.cs`, `repl-guide.md`, removed from `Program.cs`. Coding-agent CLI use proved more effective than interactive exploration; ~300 lines of unmaintained code removed. Docs updated (PRD, roadmap, README). | Cleaned up |
| 2026-02-09 | workspace-indexing-edges journal: new fixture with empty dirs, stray .txt/.md files, unknown-folder, 3 valid component types. 11 steps across 5 edge cases: empty variables/, unexpected extensions, folder→type for topics/actions/entities, type→file-candidate completion. Also found: opening .txt or unknown-folder files crashes server session (language resolver). | Passing; 12/12 journals pass |
| 2026-02-09 | Server-initiated JSON-RPC request handling: `LspClientTransport.ProcessIncomingMessage` now detects server requests (method + id) and sends JSON-RPC responses. Method-aware default policy (`[]` for workspace/configuration, `null` for others). `SetServerRequestHandler` API for custom overrides. Handler exceptions surfaced as JSON-RPC errors (-32603). New `LspJournalCli.UnitTests` test project with `FakeStdioServer` harness: 7 unit tests covering config response shape, registerCapability, interleaved client/server requests, custom handlers, error propagation, unknown methods, and notification backward compat. Integration journal (`server-request-handling`) exercises the path end-to-end. 13/13 journals pass, zero regressions. | Working; acceptance blocker resolved |
| 2026-02-09 | Notifications now matched as an order-insensitive multiset (method + normalized params). Material-change detection ignores reorder-only differences. Added multiset unit test. Forced re-record of all journals and confirmed `--all` settles. Notification distribution: 0 notifications in 183 steps, 1 notification in 69 steps. | Working; unit tests not run (runTests tool found no tests). |
| 2026-02-09 | Shutdown flakiness diagnosis (partial): Fix #1 applied to CLaSP — removed `ShutdownRequestExecutionQueueAsync()` from `Shutdown_NoLockAsync` (self-cancellation race + exit rejection). Fix exposed bug #2: shutdown handler completes in 1ms but JSON-RPC response never reaches client (stdout contention between logger and JSON-RPC writer suspected). Continuation written with full trace evidence and next steps. | Fix #1 applied; bug #2 isolated, not yet fixed |
| 2026-02-10 | Shutdown fix #2 — three product-side bugs fixed: (a) `JsonRpcStream.ProcessJsonRpcMessageAsync` silently swallowed null results for requests (shutdown returns null per LSP spec but response was never sent because `response == null` skipped `SendAsync`); fixed to always send `{"result": null, "id": N}` for requests with null handler results; (b) Default `ConsoleLoggerProvider` from `Host.CreateApplicationBuilder` wrote to stdout (same stream as JSON-RPC), corrupting `Content-Length` framing — fixed by calling `builder.Logging.ClearProviders()` in `AddLsp`; (c) `Program.cs` startup `Console.WriteLine` calls wrote to stdout — redirected to `Console.Error.WriteLine`. | Fixed; shutdown returns `result: null` in <5ms |
| 2026-02-10 | Journal CLI `JsonElement?` null round-trip fix: `"expected": null` (legit LSP null response) deserialized back as C# `null` (`HasValue=false`) causing infinite "RECORDED" loop. Root cause: `System.Text.Json` skips custom converter `Read` for null tokens when `HandleNull` is false (default). Created `NullableJsonElementConverter` with `HandleNull => true` and registered in `SerializationOptions`. Null baselines now round-trip correctly. | Fixed; all 13/13 journals pass, baselines stable |
| 2026-02-10 | LoopAndBranch missing diagnostics: root cause was fixture file mismatch. When `${file:}` support was added, `LoopAndBranch.mcs.yml` fixture had YAML-escaped quotes (`\"`) on line 16 while the original inline journal text had unescaped quotes (`"`). The backslash differences (6 extra bytes + trailing newline = 1670 vs 1662 bytes) changed the raw text the server received, causing PowerFx to parse a different expression and return empty diagnostics. Fix: overwrote fixture with the exact original byte content; updated `textHash`/`textBytes` in both affected journals. | Fixed; harness issue, not product regression |
| 2026-02-10 | Resolve LoopAndBranch completion delta: ran `all-the-things-completions` (64/64 pass, unchanged — no write). No pending deltas for LoopAndBranch completions; corrected fixture produces identical completion results. Separately noted: `completion-depth` has a pending delta where completion items now include `textEdit` fields (51 items, ~3× response size) — tracked as separate M2 work item. | Confirmed correct; no regression |
| 2026-02-10 | Diagnosis of `completion-depth` `textEdit` delta: **root cause is CRLF line endings in fixture files, not a product-side improvement.** The `DocumentTextPolicy.ExpandTextNode` reads fixture files with `File.ReadAllBytes` → `Encoding.UTF8.GetString`, preserving CRLF from Windows disk (git `text=auto` converts LF→CRLF on checkout). The original inline journal text used JSON `\n` escapes (LF-only). The LSP server splits lines on `\n` only, leaving `\r` as a trailing content character. After the `change` step clears "SetVariable" from `kind: SetVariable`, line 13 becomes `    - kind: \r` (CRLF) vs `    - kind: ` (LF). The server sees `\r` at position 12 as "existing text" and generates a `textEdit` replacing that phantom character (range `(13,12)→(13,13)`). Only step 8 is affected; all other completion steps either already had `textEdit` (real text at cursor) or had no text at cursor regardless of line endings. **Fix: normalize `\r\n` → `\n` in `DocumentTextPolicy.ExpandTextNode` to match VS Code's `didOpen` behavior. Do NOT accept the pending baseline.** | Diagnosed; fix scheduled |
| 2026-02-10 | Normalize line endings in `DocumentTextPolicy`: `ExpandTextNode` and `ScrubTextDocument` now normalize `\r\n` → `\n` before hashing and text assignment, matching VS Code's `didOpen` behavior. Both read-path (expand) and write-path (scrub) use LF-normalized bytes for SHA-256 and byte-length, so baselines are OS-independent. Existing journal hashes re-recorded via temporary relaxation of hash validation + `--all --force` + `accept --all`. `completion-depth` pending discarded. All 13/13 journals pass clean with no pending files. | Fixed; baselines OS-independent |
