mcs.metadata:
  componentName: Redirect Target
  description: This topic activates only when redirected to by another topic.
kind: AdaptiveDialog
beginDialog:
  kind: OnRedirect
  id: main
  actions:
    - kind: SendActivity
      id: sendMessage_redirect01
      activity: You were redirected to this topic.

    - kind: SendActivity
      id: sendMessage_redirect02
      activity: Performing the redirected action now.

    - kind: CancelAllDialogs
      id: cancelAllDialogs_redirect01