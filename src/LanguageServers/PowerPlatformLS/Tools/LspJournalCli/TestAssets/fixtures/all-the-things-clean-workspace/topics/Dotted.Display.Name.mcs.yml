mcs.metadata:
  componentName: Dotted.Display.Name
  description: Topic with dots in the filename — tests DotPassthrough projection.
kind: AdaptiveDialog
beginDialog:
  kind: OnRecognizedIntent
  id: main
  intent:
    displayName: Dotted Display Name
    triggerQueries:
      - test dotted name

  actions:
    - kind: SendActivity
      id: sendMessage_dotted01
      activity: This topic has dots in its filename.